# Technical-demo
# audit-api-demo

Demo: call a public REST API (Open-Meteo), build a clean pandas DataFrame, visualize it.

## Setup
python -m venv .venv
# Windows:
.venv\Scripts\activate
# macOS/Linux:
source .venv/bin/activate

pip install -r requirements.txt

## Run
python src/open_meteo_api.py